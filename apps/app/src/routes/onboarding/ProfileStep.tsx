// TEMPORARY placeholder — replaced in Task 6.
export function ProfileStep({ onDone }: { onDone: () => void }) {
  return (
    <button type="button" onClick={onDone}>
      Complete registration (placeholder — Task 6)
    </button>
  );
}
